!!!
%html

  %head
    %title
      = yield(:title) || "Untitled"
    %meta{"http-equiv"=>"Content-Type", :content=>"text/html; charset=utf-8"}/
    = stylesheet_link_tag "application"
    = javascript_include_tag "users/#{@user.id}/info" if @user
    = javascript_include_tag :defaults
    = csrf_meta_tag
    = yield(:head)
  %body
    = yield
    %footer
      &copy;
      %a{:href => 'http://mearion.blogspot.com/'} mearion